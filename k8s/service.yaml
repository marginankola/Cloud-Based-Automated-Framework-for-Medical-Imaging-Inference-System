apiVersion: v1
kind: Service
metadata:
  name: brain-tumor-service
spec:
  type: NodePort
  selector:
    app: brain-tumor
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30007
