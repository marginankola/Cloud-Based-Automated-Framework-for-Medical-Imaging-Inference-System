{
    "builds": [{
        "src": "brain_tumor_detection/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python3.9" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "brain_tumor_detection/wsgi.py"
        }
    ]
}
